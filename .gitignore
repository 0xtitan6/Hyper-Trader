__pycache__/
*.pyc
.venv/
venv/
.env
config.yaml
*.log
state/
KILL
.DS_Store
.coverage
htmlcov/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Agent worktrees (embedded git repos, never commit)
.claude/worktrees/

# scratch files from agent runs
_*.py

# Research data captures — large JSONL, regenerated by scripts, never reviewable in a PR
research/*.jsonl
research/*.json
research/*.csv

# Rotated + compressed logs (logrotate owns these)
state/*.gz
state/*.1
